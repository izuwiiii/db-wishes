{
  "name": "json-server",
  "version": "1.0.0",
  "scripts": {
    "build": "mkdir -p src/public",
    "start": "json-server --watch db.json --port 10000"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
